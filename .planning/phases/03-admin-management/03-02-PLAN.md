---
phase: 03-admin-management
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/(admin)/admin/teams/[id]/edit/page.tsx
  - app/ui/admin/TeamForm.tsx
  - app/(admin)/admin/submissions/page.tsx
autonomous: false
requirements: [ADMN-03]

must_haves:
  truths:
    - "Admin can edit a team name through a dedicated edit page"
    - "Admin can view all submissions across all teams in a single admin view"
  artifacts:
    - path: "app/(admin)/admin/teams/[id]/edit/page.tsx"
      provides: "Admin team edit page with pre-filled form"
      min_lines: 20
    - path: "app/ui/admin/TeamForm.tsx"
      provides: "Reusable client form component for team name editing"
      min_lines: 20
    - path: "app/(admin)/admin/submissions/page.tsx"
      provides: "Admin submissions overview listing all submissions"
      min_lines: 30
  key_links:
    - from: "app/(admin)/admin/teams/[id]/edit/page.tsx"
      to: "app/actions/admin-teams.ts"
      via: "updateTeam.bind(null, team.id)"
      pattern: "updateTeam\\.bind"
    - from: "app/ui/admin/TeamForm.tsx"
      to: "app/actions/admin-teams.ts"
      via: "action prop passed from edit page"
      pattern: "useActionState"
    - from: "app/(admin)/admin/submissions/page.tsx"
      to: "prisma.submission"
      via: "Prisma query with team and hackathon includes"
      pattern: "prisma\\.submission\\.findMany"
---

<objective>
Create the admin team edit page (with TeamForm client component) and the admin submissions overview page that lists all submissions across all teams.

Purpose: Completes the admin management surface — team name editing and cross-team submission visibility.
Output: 1 edit page, 1 form component, 1 submissions list page
</objective>

<execution_context>
@/home/nubroo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nubroo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-management/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — updateTeam action signature -->
From app/actions/admin-teams.ts:
```typescript
export async function updateTeam(id: string, state: unknown, formData: FormData): Promise<TeamFormState>
```

<!-- Established edit page pattern from hackathons -->
From app/(admin)/admin/hackathons/[id]/edit/page.tsx:
```typescript
export default async function EditHackathonPage({ params }: { params: Promise<{ id: string }> }) {
  await verifySession()
  const { id } = await params  // Next.js 16: params is a Promise
  const hackathon = await prisma.hackathon.findUnique({ where: { id } })
  if (!hackathon) notFound()
  const action = updateHackathon.bind(null, hackathon.id)
  return <HackathonForm action={action} hackathon={hackathon} cohorts={cohorts} />
}
```

<!-- Established form component pattern from CohortForm -->
From app/ui/admin/CohortForm.tsx (pattern):
```typescript
'use client'
import { useActionState } from 'react'
// props: action, initial values
// useActionState(action, initialState)
// form with input fields, error display, submit button
```

From app/lib/definitions.ts:
```typescript
export type TeamFormState = {
  errors?: { name?: string[] }
  message?: string
}
```

Prisma schema (Submission model):
```prisma
model Submission {
  id, teamId (unique), githubUrl, writeup, submittedAt, updatedAt
  team Team @relation(...)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TeamForm component and team edit page</name>
  <files>app/ui/admin/TeamForm.tsx, app/(admin)/admin/teams/[id]/edit/page.tsx</files>
  <read_first>
    app/ui/admin/CohortForm.tsx
    app/ui/admin/HackathonForm.tsx
    app/(admin)/admin/hackathons/[id]/edit/page.tsx
    app/(admin)/admin/cohorts/[id]/edit/page.tsx
    app/actions/admin-teams.ts
    app/lib/definitions.ts
  </read_first>
  <action>
    1. Create app/ui/admin/TeamForm.tsx as a 'use client' component:
       - Props: `{ action: (state: TeamFormState, formData: FormData) => Promise<TeamFormState>; team?: { name: string } }`
       - Import useActionState from 'react', Input from '@/components/ui/input', Button from '@/components/ui/button'
       - Import type { TeamFormState } from '@/app/lib/definitions'
       - const [state, formAction, pending] = useActionState(action, {})
       - Single form with:
         - Label "Team Name" + Input name="name" defaultValue={team?.name ?? ''} required
         - Error display: state.errors?.name && state.errors.name.map(...)
         - state.message && <p className="text-sm text-red-500">{state.message}</p>
         - Button type="submit" disabled={pending}: pending ? 'Saving...' : (team ? 'Update Team' : 'Create Team')
       - Use className="space-y-4 max-w-md" on form container
       - Match the styling patterns in CohortForm.tsx (label text-sm font-medium, error text-sm text-red-500)

    2. Create app/(admin)/admin/teams/[id]/edit/page.tsx as an async Server Component:
       - Import verifySession from '@/app/lib/dal', prisma from '@/app/lib/db', { updateTeam } from '@/app/actions/admin-teams', { notFound } from 'next/navigation', TeamForm from '@/app/ui/admin/TeamForm'
       - Export default async function EditTeamPage({ params }: { params: Promise<{ id: string }> })
       - await verifySession()
       - const { id } = await params (Next.js 16 async params)
       - const team = await prisma.team.findUnique({ where: { id } })
       - if (!team) notFound()
       - const action = updateTeam.bind(null, team.id)
       - Return layout with h1 "Edit Team" and <TeamForm action={action} team={team} />
       - Use className="p-8 space-y-6" wrapper matching other admin pages
  </action>
  <verify>
    <automated>cd /home/nubroo/bay-server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/ui/admin/TeamForm.tsx contains `'use client'` on line 1
    - app/ui/admin/TeamForm.tsx contains `useActionState(action,`
    - app/ui/admin/TeamForm.tsx contains `name="name"`
    - app/(admin)/admin/teams/[id]/edit/page.tsx contains `await verifySession()`
    - app/(admin)/admin/teams/[id]/edit/page.tsx contains `const { id } = await params`
    - app/(admin)/admin/teams/[id]/edit/page.tsx contains `updateTeam.bind(null, team.id)`
    - app/(admin)/admin/teams/[id]/edit/page.tsx contains `notFound()`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Admin team edit page at /admin/teams/[id]/edit renders TeamForm pre-filled with current team name, submits to updateTeam action</done>
</task>

<task type="auto">
  <name>Task 2: Admin submissions overview page</name>
  <files>app/(admin)/admin/submissions/page.tsx</files>
  <read_first>
    app/(admin)/admin/hackathons/page.tsx
    app/(admin)/admin/teams/page.tsx
    app/lib/dal.ts
  </read_first>
  <action>
    Create app/(admin)/admin/submissions/page.tsx as an async Server Component:

    1. Import verifySession from '@/app/lib/dal', prisma from '@/app/lib/db', Link from 'next/link', Badge from '@/components/ui/badge', format from 'date-fns'

    2. Call await verifySession() at top

    3. Fetch all submissions with team, hackathon, and cohort context:
    ```typescript
    const submissions = await prisma.submission.findMany({
      orderBy: { submittedAt: 'desc' },
      include: {
        team: {
          include: {
            hackathon: {
              include: {
                cohort: { select: { name: true, slug: true } },
              },
            },
          },
        },
      },
    })
    ```

    4. Page layout:
    - Header: "Submissions" h1 + count badge showing total submissions
    - Empty state: "No submissions yet." if submissions.length === 0
    - List container: divide-y divide-zinc-100 border border-zinc-200 rounded-lg
    - Each submission row (px-4 py-4):
      - Top line: Team name (font-medium link to /admin/teams page) + Hackathon title (Badge outline) + Cohort name (text-xs text-zinc-400)
      - Second line: GitHub URL as external link (<a href={s.githubUrl} target="_blank" rel="noopener noreferrer" className="text-sm text-blue-600 hover:underline">{s.githubUrl}</a>)
      - Third line: Writeup preview — first 150 characters of s.writeup + "..." if longer, in text-sm text-zinc-600
      - Right side: Submitted date formatted as format(s.submittedAt, 'MMM d, yyyy')

    5. Use className="p-8 space-y-6" wrapper matching other admin pages
  </action>
  <verify>
    <automated>cd /home/nubroo/bay-server && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/(admin)/admin/submissions/page.tsx contains `await verifySession()`
    - app/(admin)/admin/submissions/page.tsx contains `prisma.submission.findMany`
    - app/(admin)/admin/submissions/page.tsx contains `s.githubUrl`
    - app/(admin)/admin/submissions/page.tsx contains `s.writeup`
    - app/(admin)/admin/submissions/page.tsx contains `format(s.submittedAt,`
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 with /admin/teams/[id]/edit and /admin/submissions routes compiled
  </acceptance_criteria>
  <done>Admin submissions page shows all submissions across all teams with team name, hackathon context, GitHub link, writeup preview, and submission date</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of admin management surface</name>
  <files>none</files>
  <action>
    Human verifies the complete admin management surface built across Plan 01 and Plan 02.
  </action>
  <what-built>Complete admin management surface: teams list page with member removal, team edit page, team delete, and submissions overview</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/admin/login and log in
    2. Navigate to /admin/teams — verify teams are listed grouped by cohort and hackathon
    3. On the teams page, verify each team shows member count, submission status, and update count
    4. Verify each member has a "Remove" button next to their name
    5. Click "Edit" on a team — verify the edit page loads with the team name pre-filled
    6. Change the team name and submit — verify redirect back to /admin/teams with updated name
    7. Navigate to /admin/submissions — verify all submissions are listed with GitHub links and writeup previews
    8. Try deleting a team from /admin/teams — verify it disappears from the list
    9. Try removing a member from a team — verify member disappears and count updates
  </how-to-verify>
  <verify>Human visual inspection</verify>
  <done>All admin management features verified working: team list, team edit, team delete, member removal, submissions overview</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 — all tests pass (existing + admin-teams)
- `npm run build` exits 0 — all new routes compile: /admin/teams, /admin/teams/[id]/edit, /admin/submissions
- All admin pages call verifySession() (protected by auth)
- Submissions page shows cross-team data without cohort leakage
</verification>

<success_criteria>
- Team edit page at /admin/teams/[id]/edit pre-fills team name and submits via updateTeam
- Submissions overview at /admin/submissions lists all submissions with team/hackathon context
- All 3 success criteria from Phase 3 roadmap are met:
  1. Admin can remove a member (removeMember action + button on teams page)
  2. Admin can edit team name (edit page) and delete team (delete button on teams page)
  3. Admin can view all submissions (submissions page)
- TypeScript clean, build succeeds, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-management/03-02-SUMMARY.md`
</output>

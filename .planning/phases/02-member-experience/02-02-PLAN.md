---
phase: 02-member-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(public)/layout.tsx
  - app/(public)/page.tsx
  - app/(public)/[cohortSlug]/page.tsx
  - app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx
  - app/ui/public/TeamCard.tsx
autonomous: true
requirements: [HACK-01, HACK-02, HACK-03, HACK-04, TEAM-03, TEAM-05, DSGN-01, DSGN-02]

must_haves:
  truths:
    - "Root page lists all cohorts ordered by orderIndex descending"
    - "Cohort landing page shows all hackathon tracks with title, description, dates, and external link"
    - "Hackathon page shows live deadline countdown timer"
    - "Hackathon page lists all teams with member count and open spots"
    - "External platform link displayed as prominent button with ExternalLink icon"
    - "All pages use white Notion/Linear aesthetic with zinc text hierarchy"
    - "All pages are usable at 375px mobile viewport"
  artifacts:
    - path: "app/(public)/layout.tsx"
      provides: "Public layout with minimal header"
    - path: "app/(public)/page.tsx"
      provides: "Root cohort list page"
    - path: "app/(public)/[cohortSlug]/page.tsx"
      provides: "Cohort landing page with hackathon track cards"
    - path: "app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx"
      provides: "Hackathon detail page with countdown and team browser"
    - path: "app/ui/public/TeamCard.tsx"
      provides: "Team card component with member count, open spots, submission status"
  key_links:
    - from: "app/(public)/[cohortSlug]/page.tsx"
      to: "prisma.cohort.findUnique"
      via: "slug lookup with hackathons include"
      pattern: "prisma\\.cohort\\.findUnique"
    - from: "app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx"
      to: "prisma.hackathon.findFirst"
      via: "cohort-scoped slug lookup with teams include"
      pattern: "where.*cohortId.*cohort\\.id"
    - from: "app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx"
      to: "app/ui/public/Countdown.tsx"
      via: "Client component import with endsAt prop"
      pattern: "Countdown.*endsAt"
---

<objective>
Build all read-only public pages: root cohort list, cohort landing, and hackathon detail (with countdown timer and team browser).

Purpose: These are the pages members first see when visiting the site. They read from the database but use no Server Actions — pure Server Components (except Countdown). This plan establishes the public URL structure and visual design that Plan 03 builds interactive forms onto.

Output: Public route group with 4 pages + TeamCard component, Notion/Linear design applied
</objective>

<execution_context>
@/home/nubroo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nubroo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-member-experience/02-RESEARCH.md

<interfaces>
<!-- Countdown component created in Plan 02-01 (same wave, but may run in parallel) -->
<!-- If Plan 02-01 hasn't run yet, create a minimal Countdown stub that Plan 02-01 will overwrite -->

Expected from Plan 02-01 (app/ui/public/Countdown.tsx):
```typescript
'use client'
export function Countdown({ endsAt }: { endsAt: Date }): JSX.Element
```

From app/lib/db.ts:
```typescript
export const prisma: PrismaClient
```

From prisma/schema.prisma — query patterns needed:
- Cohort: { id, name, slug, orderIndex, hackathons[] }
- Hackathon: { id, cohortId, title, slug, description, startsAt, endsAt, externalUrl, track, teams[] }
- Team: { id, name, _count.members, submission }

Design tokens (from RESEARCH.md Pattern 7):
- Background: bg-white / bg-zinc-50
- Text: text-zinc-900 (headings), text-zinc-600 (body), text-zinc-400 (captions)
- Borders: border border-zinc-200
- Cards: rounded-lg border border-zinc-100 bg-white p-4 (no shadow)
- CTA: bg-zinc-900 text-white hover:bg-zinc-800
- Mobile: px-4 sm:px-8 gutters
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public layout + cohort list + cohort landing page</name>
  <files>app/(public)/layout.tsx, app/(public)/page.tsx, app/(public)/[cohortSlug]/page.tsx</files>
  <action>
    1. Create `app/(public)/layout.tsx`:
       - Minimal public layout — no sidebar, no admin nav
       - Header with "BAY" text logo linking to `/`, clean white background
       - `<main>` wrapper with `mx-auto max-w-3xl px-4 py-8 sm:px-8`
       - Children rendered inside main
       - Notion/Linear aesthetic: bg-white, text-zinc-900, border-b border-zinc-100 for header

    2. Create `app/(public)/page.tsx` (root page):
       - Server Component — query `prisma.cohort.findMany({ orderBy: { orderIndex: 'desc' }, include: { hackathons: true } })`
       - Render list of cohorts with name and hackathon count
       - Each cohort links to `/${cohort.slug}`
       - Style: clean list with cohort name as `text-lg font-medium text-zinc-900`, hackathon count as `text-sm text-zinc-500`
       - Empty state: "No cohorts yet" message

    3. Create `app/(public)/[cohortSlug]/page.tsx` (cohort landing):
       - Server Component — `const { cohortSlug } = await params` (Next.js 16: params is Promise)
       - Query: `prisma.cohort.findUnique({ where: { slug: cohortSlug }, include: { hackathons: { orderBy: { startsAt: 'asc' } } } })`
       - If not found, call `notFound()`
       - Render cohort name as h1, hackathon count subtitle
       - Map hackathons as cards: each card shows track (uppercase badge), title, description (line-clamp-2), date range (format with date-fns `format(date, 'MMM d, yyyy')`), and external link
       - External link: render as a small button/link with `lucide-react` `ExternalLink` icon, `target="_blank" rel="noopener noreferrer"`, stop propagation so card link still works
       - Each card links to `/${cohortSlug}/${hackathon.slug}`
       - HACK-04: External URL displayed prominently — use `bg-zinc-900 text-white text-xs px-3 py-1.5 rounded-md` button style with ExternalLink icon
       - Style per RESEARCH.md Pattern 7: white bg, zinc borders, no shadows, clean typography
       - Mobile: cards stack in flex-col, full width on small screens
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Root page lists cohorts, cohort landing page shows hackathon track cards with external links, all pages render without TypeScript errors, mobile-friendly layout</done>
</task>

<task type="auto">
  <name>Task 2: Hackathon detail page with countdown and team browser</name>
  <files>app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx, app/ui/public/TeamCard.tsx</files>
  <action>
    1. Create `app/ui/public/TeamCard.tsx`:
       - Server Component (no 'use client' needed — pure display)
       - Props: `{ team: { id: string; name: string; _count: { members: number }; submission: { id: string } | null }; cohortSlug: string; hackathonSlug: string }`
       - Render: team name, member count badge (e.g. "3/5 members"), open spots indicator
       - If `_count.members >= 5`: show "Full" badge in red-ish tone (`text-red-600 bg-red-50`)
       - If `submission !== null`: show "Submitted" badge (`text-green-600 bg-green-50`)
       - Card links to `/${cohortSlug}/${hackathonSlug}/teams/${team.id}`
       - Style: `rounded-lg border border-zinc-200 bg-white p-4 hover:border-zinc-300 transition-colors`
       - TEAM-05: Show member count, full/open status, submission status as inline badges

    2. Create `app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx`:
       - Server Component — `const { cohortSlug, hackathonSlug } = await params` (Next.js 16)
       - Query cohort by slug first: `prisma.cohort.findUnique({ where: { slug: cohortSlug } })`
       - If not found, `notFound()`
       - Query hackathon cohort-scoped: `prisma.hackathon.findFirst({ where: { slug: hackathonSlug, cohortId: cohort.id }, include: { teams: { include: { _count: { select: { members: true } }, submission: true }, orderBy: { createdAt: 'asc' } } } })`
       - CRITICAL: Always scope hackathon query with `cohortId: cohort.id` — never query by slug alone (CHRT-02 architectural pattern)
       - If not found, `notFound()`
       - Page sections:
         a. **Header**: hackathon title (h1), track badge, description, date range (format with date-fns)
         b. **External link**: Prominent CTA button — `<a href={hackathon.externalUrl} target="_blank">` with ExternalLink icon from lucide-react, styled as primary button: `inline-flex items-center gap-2 bg-zinc-900 text-white px-4 py-2 rounded-md hover:bg-zinc-800 text-sm font-medium`
         c. **Countdown**: Import `Countdown` from `@/app/ui/public/Countdown` and pass `endsAt={hackathon.endsAt}`. If Plan 02-01 hasn't created it yet, create a minimal stub: `'use client'; export function Countdown({ endsAt }: { endsAt: Date }) { return <span>Loading countdown...</span> }` — Plan 02-01 will overwrite with real implementation.
         d. **Team browser**: heading "Teams" with count, then grid/list of TeamCard components. Show "Create a Team" link/button to `/${cohortSlug}/${hackathonSlug}/teams/new` styled as secondary button
         e. **Empty state**: If no teams, show "No teams yet. Be the first to create one!" with the create team link
       - HACK-03: Countdown is visible and prominent near the top
       - TEAM-03: All teams listed with member count and open spots via TeamCard
       - Mobile: single column layout, team cards stack vertically
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Hackathon page renders countdown timer, team browser with member counts and status badges, external link as prominent CTA, cohort-scoped query enforced</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
npm run build
Both commands exit 0. All public pages compile and build without errors.
</verification>

<success_criteria>
- Root page at / lists all cohorts ordered by orderIndex descending
- Cohort landing at /[cohortSlug] shows hackathon track cards with external links
- Hackathon page at /[cohortSlug]/[hackathonSlug] shows countdown, team browser, and external CTA
- All queries are cohort-scoped (hackathon query uses cohortId)
- TeamCard shows member count, full/open status, and submission badge
- Notion/Linear aesthetic: white bg, zinc text hierarchy, no shadows, clean borders
- Mobile-usable at 375px: single column, adequate tap targets
- All params are awaited (Next.js 16)
</success_criteria>

<output>
After completion, create `.planning/phases/02-member-experience/02-02-SUMMARY.md`
</output>

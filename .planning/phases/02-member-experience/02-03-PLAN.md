---
phase: 02-member-experience
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx
  - app/ui/public/CreateTeamForm.tsx
  - app/ui/public/JoinTeamForm.tsx
  - app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx
autonomous: true
requirements: [TEAM-01, TEAM-02, TEAM-04, PROG-02]

must_haves:
  truths:
    - "Member can create a new team by entering only a team name"
    - "Member can join an existing team by entering name and role"
    - "6th join attempt is rejected with clear error message"
    - "Team detail page shows all members with name and role"
    - "Team detail page shows chronological weekly updates"
    - "Team detail page shows submission if submitted"
  artifacts:
    - path: "app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx"
      provides: "Create team page with form"
    - path: "app/ui/public/CreateTeamForm.tsx"
      provides: "Client form component for team creation"
    - path: "app/ui/public/JoinTeamForm.tsx"
      provides: "Client form component for joining a team"
    - path: "app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx"
      provides: "Team detail page with members, updates, submission, and join form"
  key_links:
    - from: "app/ui/public/CreateTeamForm.tsx"
      to: "app/actions/teams.ts"
      via: "useActionState with createTeam.bind"
      pattern: "createTeam"
    - from: "app/ui/public/JoinTeamForm.tsx"
      to: "app/actions/members.ts"
      via: "useActionState with joinTeam.bind"
      pattern: "joinTeam"
    - from: "app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx"
      to: "prisma.team.findUnique"
      via: "include members, updates, submission, hackathon.cohort"
      pattern: "prisma\\.team\\.findUnique"
---

<objective>
Build the interactive team pages: create team form, team detail page (members + updates + submission display), and join team form.

Purpose: This is the core member interaction surface — where members create teams, join teams, and see their team's progress. The create/join flows require NO login (zero-friction). The team detail page is the central hub showing everything about a team.

Output: Create team page, team detail page, CreateTeamForm and JoinTeamForm client components
</objective>

<execution_context>
@/home/nubroo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nubroo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-member-experience/02-RESEARCH.md
@.planning/phases/02-member-experience/02-01-SUMMARY.md
@.planning/phases/02-member-experience/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 02-01: Server Actions -->
From app/actions/teams.ts:
```typescript
export async function createTeam(
  context: { hackathonId: string; cohortSlug: string; hackathonSlug: string },
  state: unknown,
  formData: FormData
): Promise<{ errors?: { name?: string[] } } | void>
```

From app/actions/members.ts:
```typescript
export async function joinTeam(
  context: { teamId: string; cohortSlug: string; hackathonSlug: string },
  state: unknown,
  formData: FormData
): Promise<{ errors?: Partial<Record<'name' | 'role', string[]>>; error?: string } | void>
```

From app/lib/definitions.ts:
```typescript
export type TeamFormState = { errors?: { name?: string[] }; message?: string }
export type MemberFormState = { errors?: Partial<Record<'name' | 'role', string[]>>; error?: string }
```

<!-- From Plan 02-02: Public page URL structure -->
URL structure:
- /[cohortSlug]/[hackathonSlug]/teams/new — Create team page
- /[cohortSlug]/[hackathonSlug]/teams/[teamId] — Team detail page

Prisma Team model includes:
- members: Member[] (orderBy: { joinedAt: 'asc' })
- updates: Update[] (orderBy: { createdAt: 'asc' })
- submission: Submission | null
- hackathon: { slug, cohort: { slug } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create team page + CreateTeamForm</name>
  <files>app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx, app/ui/public/CreateTeamForm.tsx</files>
  <action>
    1. Create `app/ui/public/CreateTeamForm.tsx`:
       - `'use client'`
       - Import `useActionState` from `react`
       - Import `createTeam` from `@/app/actions/teams`
       - Props: `{ hackathonId: string; cohortSlug: string; hackathonSlug: string }`
       - Bind the action: `const boundAction = createTeam.bind(null, { hackathonId, cohortSlug, hackathonSlug })`
       - `const [state, formAction, isPending] = useActionState(boundAction, null)`
       - Form fields: team name input (text, required)
       - Display validation errors from `state?.errors?.name`
       - Submit button with pending state: "Creating..." when isPending, "Create Team" otherwise
       - Style: clean Notion aesthetic — white bg, zinc borders, full-width input, zinc-900 submit button
       - Input: `border border-zinc-200 rounded-md px-3 py-2 text-sm w-full focus:outline-none focus:ring-2 focus:ring-zinc-900`
       - Error text: `text-red-600 text-sm mt-1`

    2. Create `app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx`:
       - Server Component — `const { cohortSlug, hackathonSlug } = await params`
       - Query cohort by slug, then hackathon by slug + cohortId (cohort-scoped, same as hackathon page)
       - If not found, `notFound()`
       - Render: heading "Create a Team", back link to hackathon page, then CreateTeamForm with hackathonId + slugs
       - Style: centered layout, max-w-md, clean white form card
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Create team page renders form, CreateTeamForm binds createTeam action with correct context, validation errors display</done>
</task>

<task type="auto">
  <name>Task 2: Team detail page + JoinTeamForm</name>
  <files>app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx, app/ui/public/JoinTeamForm.tsx</files>
  <action>
    1. Create `app/ui/public/JoinTeamForm.tsx`:
       - `'use client'`
       - Import `useActionState` from `react`
       - Import `joinTeam` from `@/app/actions/members`
       - Props: `{ teamId: string; cohortSlug: string; hackathonSlug: string; isFull: boolean }`
       - Bind: `joinTeam.bind(null, { teamId, cohortSlug, hackathonSlug })`
       - `const [state, formAction, isPending] = useActionState(boundAction, null)`
       - Form fields: name (text), role (text) — both required
       - Display field-level errors from `state?.errors?.name` and `state?.errors?.role`
       - Display general error from `state?.error` (e.g. "This team is full")
       - If `isFull` prop is true, disable the form entirely: show "This team is full" message, disable inputs and button
       - Submit: "Join Team" / "Joining..." pending state
       - Style: same clean Notion aesthetic as CreateTeamForm

    2. Create `app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx`:
       - Server Component — `const { cohortSlug, hackathonSlug, teamId } = await params`
       - Query: `prisma.team.findUnique({ where: { id: teamId }, include: { members: { orderBy: { joinedAt: 'asc' } }, updates: { orderBy: { createdAt: 'asc' } }, submission: true, hackathon: { include: { cohort: true } } } })`
       - If not found, `notFound()`
       - Verify cohort scope: `if (team.hackathon.slug !== hackathonSlug || team.hackathon.cohort.slug !== cohortSlug) notFound()`
       - Compute: `const isFull = team.members.length >= 5; const openSpots = 5 - team.members.length`
       - Page sections:
         a. **Breadcrumb**: cohort > hackathon > team name (links back)
         b. **Team header**: team name (h1), member count badge ("3/5 members"), status badges (Full/Open, Submitted/Not submitted)
         c. **Members list**: ordered table/list — name, role, joined date (format with date-fns `format(date, 'MMM d, yyyy')`)
         d. **Join form**: JoinTeamForm component — only show if NOT full. Pass isFull prop for safety.
         e. **Weekly updates section**: heading "Updates", chronological list of updates — each shows content, optional link (as clickable URL), and created date. If no updates: "No updates yet."
         f. **Submission section**: If submission exists, show GitHub URL (clickable link), writeup text, submitted date. If no submission: "No submission yet."
       - PROG-02: Updates displayed chronologically (orderBy createdAt asc from query)
       - TEAM-04: All members, updates, and submission visible
       - Style: Notion/Linear — sections separated by `border-t border-zinc-100 pt-6 mt-6`, member list as clean rows, updates as simple cards
       - Mobile: single column, all sections stack naturally
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Team detail page shows members, updates, submission; JoinTeamForm handles full team state; cohort scope verified on every request</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
npm run build
TypeScript clean, all routes compile. Team pages are properly linked from hackathon page (via TeamCard links from Plan 02-02).
</verification>

<success_criteria>
- Create team page at /[cohortSlug]/[hackathonSlug]/teams/new with name-only form
- Team detail page at /[cohortSlug]/[hackathonSlug]/teams/[teamId] with members, updates, submission
- JoinTeamForm disables when team is full, shows error on 6th attempt
- Updates displayed in chronological order
- Submission displayed if exists, "No submission yet" otherwise
- Cohort scope verified on team detail page (hackathon.cohort.slug check)
- All params awaited (Next.js 16)
- Clean Notion/Linear aesthetic maintained
</success_criteria>

<output>
After completion, create `.planning/phases/02-member-experience/02-03-SUMMARY.md`
</output>

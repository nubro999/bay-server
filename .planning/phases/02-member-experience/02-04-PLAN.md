---
phase: 02-member-experience
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - app/ui/public/UpdateForm.tsx
  - app/ui/public/SubmissionForm.tsx
  - app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx
autonomous: false
requirements: [PROG-01, PROG-02, PROG-03, DSGN-01, DSGN-02]

must_haves:
  truths:
    - "Any visitor can post a weekly progress update from the team detail page"
    - "Any visitor can submit the final project from the team detail page"
    - "Submitting twice updates the existing submission (upsert behavior)"
    - "All public pages use minimal white Notion/Linear aesthetic"
    - "All public pages are usable on mobile at 375px viewport"
  artifacts:
    - path: "app/ui/public/UpdateForm.tsx"
      provides: "Client form for posting progress updates"
    - path: "app/ui/public/SubmissionForm.tsx"
      provides: "Client form for final project submission"
  key_links:
    - from: "app/ui/public/UpdateForm.tsx"
      to: "app/actions/updates.ts"
      via: "useActionState with createUpdate.bind"
      pattern: "createUpdate"
    - from: "app/ui/public/SubmissionForm.tsx"
      to: "app/actions/submissions.ts"
      via: "useActionState with upsertSubmission.bind"
      pattern: "upsertSubmission"
    - from: "app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx"
      to: "UpdateForm and SubmissionForm"
      via: "Component imports wired into team detail sections"
      pattern: "UpdateForm|SubmissionForm"
---

<objective>
Add progress update and submission forms to the team detail page, then verify the complete visual design across all public pages.

Purpose: This completes the member interaction surface — team members can now post weekly updates and submit their final project. The visual checkpoint ensures the Notion/Linear aesthetic and mobile responsiveness meet the design requirements.

Output: UpdateForm + SubmissionForm client components wired into team detail page, visual verification of all public pages
</objective>

<execution_context>
@/home/nubroo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nubroo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-member-experience/02-RESEARCH.md
@.planning/phases/02-member-experience/02-03-SUMMARY.md

<interfaces>
From app/actions/updates.ts (created in Plan 02-01):
```typescript
export async function createUpdate(
  context: { teamId: string; cohortSlug: string; hackathonSlug: string },
  state: unknown,
  formData: FormData
): Promise<{ errors?: Partial<Record<'content' | 'link', string[]>> } | void>
```

From app/actions/submissions.ts (created in Plan 02-01):
```typescript
export async function upsertSubmission(
  context: { teamId: string; cohortSlug: string; hackathonSlug: string },
  state: unknown,
  formData: FormData
): Promise<{ errors?: Partial<Record<'githubUrl' | 'writeup', string[]>> } | void>
```

From app/lib/definitions.ts:
```typescript
export type UpdateFormState = { errors?: Partial<Record<'content' | 'link', string[]>> }
export type SubmissionFormState = { errors?: Partial<Record<'githubUrl' | 'writeup', string[]>> }
```

Team detail page (from Plan 02-03) already has:
- Members section
- Join team form
- Updates display section (read-only)
- Submission display section (read-only)
- Will be modified to add UpdateForm and SubmissionForm components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UpdateForm + SubmissionForm + wire into team detail</name>
  <files>app/ui/public/UpdateForm.tsx, app/ui/public/SubmissionForm.tsx, app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx</files>
  <action>
    1. Create `app/ui/public/UpdateForm.tsx`:
       - `'use client'`
       - Import `useActionState` from `react`, `createUpdate` from `@/app/actions/updates`
       - Props: `{ teamId: string; cohortSlug: string; hackathonSlug: string }`
       - Bind: `createUpdate.bind(null, { teamId, cohortSlug, hackathonSlug })`
       - `const [state, formAction, isPending] = useActionState(boundAction, null)`
       - Fields:
         a. `content` — textarea, required, placeholder "What did your team accomplish this week?"
         b. `link` — text input, optional, placeholder "https://... (optional link)"
       - Validation errors: show per-field from state.errors
       - Submit: "Post Update" / "Posting..." pending state
       - Style: textarea `min-h-[100px] resize-y`, same input styling as other forms

    2. Create `app/ui/public/SubmissionForm.tsx`:
       - `'use client'`
       - Import `useActionState` from `react`, `upsertSubmission` from `@/app/actions/submissions`
       - Props: `{ teamId: string; cohortSlug: string; hackathonSlug: string; existingSubmission?: { githubUrl: string; writeup: string } | null }`
       - Bind: `upsertSubmission.bind(null, { teamId, cohortSlug, hackathonSlug })`
       - `const [state, formAction, isPending] = useActionState(boundAction, null)`
       - Fields:
         a. `githubUrl` — text input, required, placeholder "https://github.com/your-repo", defaultValue from existingSubmission
         b. `writeup` — textarea, required, placeholder "Describe your project, what you built, and key features...", defaultValue from existingSubmission
       - If existingSubmission exists, show "Update Submission" as button text instead of "Submit Project"
       - Validation errors per field
       - Style: textarea `min-h-[120px] resize-y`

    3. Modify `app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx`:
       - Import UpdateForm and SubmissionForm
       - In the **Updates section**: after the existing update list, add the UpdateForm component below with a "Post an Update" heading
       - In the **Submission section**: after the existing submission display, add the SubmissionForm component. Pass `existingSubmission={team.submission}` so it pre-fills if a submission exists
       - The forms should be visually separated from the display sections with a subtle border-t or spacing
       - Both forms are always visible (no auth gate — zero-friction)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>UpdateForm and SubmissionForm render on team detail page, validation errors display, existing submission pre-fills form, all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all public pages</name>
  <files>N/A</files>
  <action>
    User verifies the complete member-facing UI in browser.
    What was built: cohort list, cohort landing (hackathon tracks), hackathon page (countdown + team browser), create team, team detail (members + join + updates + submission).
    Verification steps:
    1. Start dev server: `npm run dev`
    2. Open http://localhost:3000 — verify cohort list page renders
    3. If seed data exists in DB:
       a. Navigate to a cohort page — verify hackathon cards show track, title, description, date range, external link button
       b. Click a hackathon — verify countdown timer ticks, team list shows member counts, "Create a Team" button visible
       c. Click "Create a Team" — verify form works (enter team name, submit)
       d. On team detail page — verify members list, join form, update form, submission form all visible
       e. Try joining a team (enter name + role)
       f. Try posting an update (enter text + optional link)
       g. Try submitting a project (enter GitHub URL + writeup)
    4. Open DevTools -> toggle device toolbar -> set to 375px width
       a. Verify all pages are usable — no horizontal scroll, tap targets adequate, text readable
    5. Check visual aesthetics: white background, clean typography, zinc color palette, no shadows on cards, subtle borders
    6. Verify: external hackathon links open in new tab with correct URL
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User has approved the visual design (DSGN-01) and mobile responsiveness (DSGN-02) of all public pages</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
npx vitest run
npm run build
All pass. Visual verification by user in checkpoint.
</verification>

<success_criteria>
- UpdateForm on team detail page allows posting progress updates
- SubmissionForm on team detail page allows submitting/updating final project
- Existing submission pre-fills the form fields (upsert UX)
- All public pages visually match Notion/Linear aesthetic (DSGN-01)
- All public pages usable at 375px mobile viewport (DSGN-02)
- User has approved the visual and functional checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-member-experience/02-04-SUMMARY.md`
</output>

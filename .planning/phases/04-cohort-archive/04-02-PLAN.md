---
phase: 04-cohort-archive
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx
  - app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx
  - app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx
autonomous: false
requirements: [CHRT-03]

must_haves:
  truths:
    - "No create/join/update/submit actions are available on archived cohort pages"
    - "Hackathon page for archived cohort hides the Create a Team button"
    - "Team detail page for archived cohort hides Join, Update, and Submission forms"
    - "Create team page for archived cohort redirects or shows read-only notice"
    - "All existing data (teams, members, updates, submissions) remains visible on archived pages"
  artifacts:
    - path: "app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx"
      provides: "Hackathon page with Create a Team button hidden for archived cohorts"
      contains: "isActive"
    - path: "app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx"
      provides: "Create team page that blocks creation for archived cohorts"
      contains: "isActive"
    - path: "app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx"
      provides: "Team detail page with forms hidden for archived cohorts"
      contains: "isActive"
  key_links:
    - from: "app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx"
      to: "prisma.cohort.isActive"
      via: "cohort query already in page"
      pattern: "cohort\\.isActive"
    - from: "app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx"
      to: "team.hackathon.cohort.isActive"
      via: "existing include chain"
      pattern: "cohort\\.isActive"
---

<objective>
Enforce read-only behavior on all public pages for archived cohorts by hiding create/join/update/submit actions while keeping all existing data visible.

Purpose: Complete the archive experience — users can browse past cohort data but cannot modify it.
Output: All public interaction points gated by cohort.isActive check.
</objective>

<execution_context>
@/home/nubroo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nubroo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cohort-archive/04-01-SUMMARY.md

@app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx
@app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx
@app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx
@app/ui/public/JoinTeamForm.tsx
@app/ui/public/UpdateForm.tsx
@app/ui/public/SubmissionForm.tsx
@app/ui/public/CreateTeamForm.tsx

<interfaces>
<!-- From prisma/schema.prisma (after Plan 01) -->
model Cohort {
  id          String      @id @default(cuid())
  name        String
  slug        String      @unique
  orderIndex  Int         @default(0)
  isActive    Boolean     @default(true)
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  hackathons  Hackathon[]
  @@map("cohorts")
}

<!-- Hackathon page already queries cohort separately, then hackathon with teams -->
<!-- Team detail page includes hackathon.cohort via include chain -->
<!-- Create team page queries cohort separately -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide all action UI on archived cohort pages</name>
  <files>app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx, app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx, app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx</files>
  <read_first>app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx, app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx, app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx, app/ui/public/JoinTeamForm.tsx, app/ui/public/UpdateForm.tsx, app/ui/public/SubmissionForm.tsx</read_first>
  <action>
1. **Hackathon page** (app/(public)/[cohortSlug]/[hackathonSlug]/page.tsx):
   - The page already queries `cohort` via `prisma.cohort.findUnique`. Use `cohort.isActive` (Prisma will include it automatically since it's on the model).
   - Wrap both "Create a Team" links (the one in the header and the one in the empty state) in `{cohort.isActive && ( ... )}` conditionals.
   - After the countdown section (before team browser), if `!cohort.isActive`, render the same amber archive banner used on the cohort landing page:
     ```tsx
     {!cohort.isActive && (
       <div className="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 mb-6">
         <p className="text-sm text-amber-800">
           This cohort has been archived. You are viewing a read-only record.
         </p>
       </div>
     )}
     ```
   - Keep all team cards visible (read-only browsing is the goal).

2. **Create team page** (app/(public)/[cohortSlug]/[hackathonSlug]/teams/new/page.tsx):
   - The page already queries `cohort`. After the cohort null check, add:
     ```tsx
     if (!cohort.isActive) {
       notFound()
     }
     ```
   - This returns a 404 for archived cohorts — there's no reason to show a create form. The "Create a Team" link won't be visible anyway (hidden in hackathon page), but this guards against direct URL access.

3. **Team detail page** (app/(public)/[cohortSlug]/[hackathonSlug]/teams/[teamId]/page.tsx):
   - The page already includes `hackathon: { include: { cohort: true } }`. Extract `const isArchived = !team.hackathon.cohort.isActive` after the cohort scope verification.
   - Conditionally hide the **Join form section**: wrap the entire "Join this Team" div (border-t section containing h2 "Join this Team" and the JoinTeamForm) in `{!isArchived && ( ... )}`.
   - Conditionally hide the **Post an Update section**: wrap the entire "Post an Update" div (border-t section containing h2 "Post an Update" and the UpdateForm) in `{!isArchived && ( ... )}`.
   - Conditionally hide the **Submit/Update Submission form**: keep the Submission display section (showing existing submission data) but wrap only the form section (the border-t div containing "Update Submission" / "Submit Project" heading and SubmissionForm) in `{!isArchived && ( ... )}`.
   - Keep ALL read-only data visible: members list, updates list, existing submission display.
   - After the breadcrumb nav, if isArchived, render the amber archive banner:
     ```tsx
     {isArchived && (
       <div className="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 mb-6">
         <p className="text-sm text-amber-800">
           This cohort has been archived. You are viewing a read-only record.
         </p>
       </div>
     )}
     ```
  </action>
  <verify>
    <automated>cd /home/nubroo/bay-server && grep -n "isActive\|isArchived" app/\(public\)/\[cohortSlug\]/\[hackathonSlug\]/page.tsx app/\(public\)/\[cohortSlug\]/\[hackathonSlug\]/teams/new/page.tsx app/\(public\)/\[cohortSlug\]/\[hackathonSlug\]/teams/\[teamId\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- Hackathon page conditionally hides "Create a Team" links when cohort.isActive is false
- Hackathon page shows amber archive banner when cohort is archived
- Create team page returns notFound() when cohort.isActive is false
- Team detail page defines isArchived = !team.hackathon.cohort.isActive
- Team detail page hides "Join this Team" section when isArchived
- Team detail page hides "Post an Update" section when isArchived
- Team detail page hides submission form section when isArchived
- Team detail page keeps members list, updates list, and existing submission visible when archived
- Team detail page shows amber archive banner when isArchived
- grep for "isActive" or "isArchived" returns matches in all three page files
  </acceptance_criteria>
  <done>All public action forms and buttons are hidden for archived cohorts; all read-only data remains visible; archive notice banners displayed; direct URL to create team returns 404</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of archive behavior</name>
  <files>none</files>
  <action>
Human visually verifies the complete cohort archive system across admin and public pages. No code changes in this task — verification only.
  </action>
  <verify>
    <automated>echo "Checkpoint: manual verification required"</automated>
  </verify>
  <done>User confirms: archived cohorts show read-only across all pages, active cohorts retain all forms, admin toggle works both directions</done>
  <what-built>Complete cohort archive system: schema field, admin toggle, visual distinction on public pages, read-only enforcement on all action points (create team, join team, post update, submit project)</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Log in to admin at /admin/login
    3. Go to /admin/cohorts — verify each cohort row has an "Archive" button
    4. Click "Archive" on a cohort — verify the button changes to "Unarchive" and an "Archived" badge appears
    5. Visit the public home page (/) — verify active cohorts appear first, then a "Past Cohorts" divider, then the archived cohort dimmed with "Archived" badge
    6. Click into the archived cohort — verify the amber "This cohort has been archived" banner appears
    7. Click into a hackathon within the archived cohort — verify no "Create a Team" button is visible and the amber archive banner appears
    8. Click into a team within the archived cohort — verify:
       - Members list, updates, and submission are visible (read-only)
       - No "Join this Team" form
       - No "Post an Update" form
       - No "Submit Project" / "Update Submission" form
       - Amber archive banner appears
    9. Try navigating directly to /{archived-cohort-slug}/{hackathon-slug}/teams/new — verify it returns 404
    10. Click "Unarchive" in admin — verify the cohort returns to active state with all forms restored
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All public action points gated by isActive check
- Archived cohort shows read-only notice on every page level (cohort landing, hackathon, team detail)
- Create team page returns 404 for archived cohorts
- All existing data (members, updates, submissions) remains visible
- Admin can toggle archive status back and forth
</verification>

<success_criteria>
1. Navigating to a past cohort shows its hackathons, teams, updates, and submissions in read-only form
2. No create/join/update actions are available on archived cohort pages
3. The active cohort and archived cohorts are visually distinct so members cannot confuse them
</success_criteria>

<output>
After completion, create `.planning/phases/04-cohort-archive/04-02-SUMMARY.md`
</output>
